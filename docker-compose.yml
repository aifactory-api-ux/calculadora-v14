version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - VITE_APP_TITLE=Calculadora
      - VITE_APP_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev